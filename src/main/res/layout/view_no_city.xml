<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="32dp">

    <!-- Image -->
    <ImageView
        android:id="@+id/ivNoCity"
        android:layout_width="162dp"
        android:layout_height="162dp"
        android:contentDescription="No City Available"
        android:src="@drawable/im_no_city"
        app:layout_constraintBottom_toTopOf="@id/tvTitle"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintVertical_chainStyle="packed" />

    <!-- Title -->
    <TextView
        android:id="@+id/tvTitle"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="24dp"
        android:fontFamily="@font/bold"
        android:gravity="center"
        android:textColor="@color/trp_timeline_title_color"
        android:textSize="20sp"
        app:layout_constraintBottom_toTopOf="@id/tvDescription"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/ivNoCity"
        tools:text="We haven't landed here yet" />

    <!-- Description -->
    <TextView
        android:id="@+id/tvDescription"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="12dp"
        android:fontFamily="@font/regular"
        android:gravity="center"
        android:textColor="@color/trp_timeline_subtitle_color"
        android:textSize="16sp"
        app:layout_constraintBottom_toTopOf="@id/btnGoToMyTrip"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/tvTitle"
        tools:text="This feature is not available for this destination yet." />

    <!-- Button -->
    <com.google.android.material.button.MaterialButton
        android:id="@+id/btnGoToMyTrip"
        style="@style/Widget.MaterialComponents.Button.OutlinedButton"
        android:layout_width="wrap_content"
        android:layout_height="48dp"
        android:layout_marginTop="32dp"
        android:paddingHorizontal="32dp"
        android:textAllCaps="false"
        android:textColor="@color/trp_primary"
        android:textSize="16sp"
        app:cornerRadius="24dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/tvDescription"
        app:strokeColor="@color/trp_primary"
        app:strokeWidth="1dp"
        tools:text="Go to My Trip" />

</androidx.constraintlayout.widget.ConstraintLayout>
