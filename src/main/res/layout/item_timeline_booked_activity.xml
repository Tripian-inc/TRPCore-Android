<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="?attr/selectableItemBackground"
    android:orientation="vertical"
    android:paddingTop="20dp"
    android:paddingHorizontal="16dp">

    <!-- Order-Time Container (130x32, border) -->
    <LinearLayout
        android:id="@+id/orderTimeContainer"
        android:layout_width="wrap_content"
        android:layout_height="32dp"
        android:background="@drawable/bg_order_time_container"
        android:gravity="center_vertical"
        android:orientation="horizontal">

        <!-- Order Badge (20x20, fg bg, 6px left margin) -->
        <TextView
            android:id="@+id/tvOrder"
            android:layout_width="20dp"
            android:layout_height="20dp"
            android:layout_marginStart="6dp"
            android:background="@drawable/bg_step_order_new"
            android:fontFamily="@font/semibold"
            android:gravity="center"
            android:includeFontPadding="false"
            android:textColor="@color/trp_white"
            android:textSize="16sp"
            tools:text="1" />

        <!-- Time (startTime - endTime, 10px right margin) -->
        <TextView
            android:id="@+id/tvTime"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginHorizontal="10dp"
            android:layout_weight="1"
            android:fontFamily="@font/medium"
            android:gravity="end"
            android:textColor="@color/trp_text_primary"
            android:textSize="14sp"
            tools:text="09:00 - 11:30" />

    </LinearLayout>

    <!-- Vertical Line (1x24, 16px left margin, connected to container) -->
    <View
        android:id="@+id/verticalLine"
        android:layout_width="1dp"
        android:layout_height="24dp"
        android:layout_marginStart="16dp"
        android:background="@color/trp_lineWeak" />

    <!-- Content Row (Image + Details) - No CardView -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal">

        <!-- Image 80x80, 4dp corner radius -->
        <com.google.android.material.card.MaterialCardView
            android:layout_width="80dp"
            android:layout_height="80dp"
            app:cardCornerRadius="4dp"
            app:cardElevation="0dp">

            <ImageView
                android:id="@+id/ivImage"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:contentDescription="Activity Image"
                android:scaleType="centerCrop" />

        </com.google.android.material.card.MaterialCardView>

        <!-- Text Content -->
        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="12dp"
            android:layout_weight="1"
            android:orientation="vertical">

            <!-- Title -->
            <TextView
                android:id="@+id/tvTitle"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:ellipsize="end"
                android:fontFamily="@font/semibold"
                android:maxLines="2"
                android:textColor="@color/trp_text_primary"
                android:textSize="15sp"
                tools:text="Visita guiada por la Basílica de San Pedro" />

            <!-- Confirmed Badge - green bg, green text, medium 12px -->
            <TextView
                android:id="@+id/tvBadge"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="6dp"
                android:background="@drawable/bg_confirmed_badge"
                android:fontFamily="@font/medium"
                android:paddingHorizontal="6dp"
                android:paddingVertical="4dp"
                android:textColor="@color/trp_confirmed_badge_text"
                android:textSize="12sp"
                tools:text="Confirmed" />

            <!-- Travelers Row - icon 16x16, Light 14px primaryText -->
            <LinearLayout
                android:id="@+id/llTravelers"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="6dp"
                android:gravity="center_vertical"
                android:orientation="horizontal">

                <ImageView
                    android:layout_width="16dp"
                    android:layout_height="16dp"
                    android:contentDescription="Travelers"
                    android:src="@drawable/ic_traveler_count" />

                <TextView
                    android:id="@+id/tvTravelers"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginStart="4dp"
                    android:fontFamily="@font/regular"
                    android:textColor="@color/trp_text_primary"
                    android:textSize="14sp"
                    tools:text="2 Adults, 1 child" />

            </LinearLayout>

            <!-- Cancellation - Medium 14px, advantage color -->
            <TextView
                android:id="@+id/tvCancellation"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="4dp"
                android:fontFamily="@font/medium"
                android:textColor="@color/trp_advantage"
                android:textSize="14sp"
                tools:text="Free cancellation" />

        </LinearLayout>

    </LinearLayout>

    <!-- Hidden fields for ViewHolder compatibility -->
    <TextView
        android:id="@+id/tvDate"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:visibility="gone" />

    <LinearLayout
        android:id="@+id/llDurationPrice"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:orientation="horizontal"
        android:visibility="gone">

        <TextView
            android:id="@+id/tvDuration"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content" />

        <TextView
            android:id="@+id/tvPrice"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content" />

    </LinearLayout>

    <TextView
        android:id="@+id/tvDescription"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:visibility="gone" />

    <com.google.android.material.button.MaterialButton
        android:id="@+id/btnReservation"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:visibility="gone" />

    <ImageButton
        android:id="@+id/btnDelete"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:visibility="gone" />

</LinearLayout>
