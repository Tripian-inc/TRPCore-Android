<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/trp_white">

    <!-- Header -->
    <androidx.constraintlayout.widget.ConstraintLayout
        android:id="@+id/headerContainer"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/trp_white"
        android:paddingHorizontal="16dp"
        android:paddingVertical="16dp"
        app:layout_constraintTop_toTopOf="parent">

        <ImageView
            android:id="@+id/ivBack"
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:contentDescription="Back"
            android:src="@drawable/ic_back"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <TextView
            android:id="@+id/tvTitle"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:fontFamily="@font/bold"
            android:gravity="center"
            android:paddingHorizontal="24dp"
            android:textColor="@color/trp_timeline_title_color"
            android:textSize="18sp"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

    </androidx.constraintlayout.widget.ConstraintLayout>

    <!-- Saved Plans Card -->
    <androidx.cardview.widget.CardView
        android:id="@+id/btnSavedPlans"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginHorizontal="16dp"
        android:visibility="gone"
        app:cardBackgroundColor="@color/trp_neutral100"
        app:cardCornerRadius="8dp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/headerContainer"
        tools:visibility="visible">

        <androidx.constraintlayout.widget.ConstraintLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:paddingHorizontal="16dp"
            android:paddingVertical="16dp">

            <!-- Heart Icon Container with Badge -->
            <FrameLayout
                android:id="@+id/flHeartContainer"
                android:layout_width="42dp"
                android:layout_height="42.5dp"
                app:layout_constraintBottom_toBottomOf="parent"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintTop_toTopOf="parent">

                <!-- Heart Icon Background -->
                <com.tripian.trpcore.util.widget.ImageView
                    android:id="@+id/ivHeartBg"
                    android:layout_width="40dp"
                    android:layout_height="40dp"
                    android:background="@drawable/bg_circle_pink"
                    android:contentDescription="Heart"
                    android:padding="10dp"
                    android:src="@drawable/ic_heart" />

                <!-- Badge -->
                <TextView
                    android:id="@+id/tvSavedPlansBadge"
                    android:layout_width="16dp"
                    android:layout_height="16dp"
                    android:layout_gravity="bottom|end"
                    android:background="@drawable/bg_circle_primary"
                    android:fontFamily="@font/semibold"
                    android:gravity="center"
                    android:includeFontPadding="false"
                    android:textColor="@color/trp_white"
                    android:textSize="11sp"
                    tools:text="3" />

            </FrameLayout>

            <!-- Saved Plans Text -->
            <TextView
                android:id="@+id/tvSavedPlansText"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_marginStart="11dp"
                android:layout_marginEnd="16dp"
                android:fontFamily="@font/medium"
                android:textColor="@color/trp_text_primary"
                android:textSize="14sp"
                app:layout_constraintBottom_toBottomOf="parent"
                app:layout_constraintEnd_toStartOf="@id/ivSavedPlansArrow"
                app:layout_constraintStart_toEndOf="@id/flHeartContainer"
                app:layout_constraintTop_toTopOf="parent"
                tools:text="Añade tus planes guardados al itinerario" />

            <!-- Arrow Icon -->
            <ImageView
                android:id="@+id/ivSavedPlansArrow"
                android:layout_width="20dp"
                android:layout_height="20dp"
                android:contentDescription="Go"
                android:src="@drawable/ic_saved_plans_next"
                app:layout_constraintBottom_toBottomOf="parent"
                app:layout_constraintEnd_toEndOf="parent"
                app:layout_constraintTop_toTopOf="parent" />

        </androidx.constraintlayout.widget.ConstraintLayout>

    </androidx.cardview.widget.CardView>

    <!-- Day Filter - elevated to appear above map -->
    <com.tripian.trpcore.ui.timeline.views.TimelineDayFilterView
        android:id="@+id/dayFilterView"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/_6sdp"
        android:background="@color/trp_transparent"
        app:layout_constraintTop_toBottomOf="@id/btnSavedPlans" />

    <!-- SwipeRefreshLayout with RecyclerView -->
    <androidx.swiperefreshlayout.widget.SwipeRefreshLayout
        android:id="@+id/swipeRefresh"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintTop_toBottomOf="@id/dayFilterView">

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/rvTimeline"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="@color/trp_white"
            android:clipToPadding="false"
            android:paddingBottom="120dp"
            tools:listitem="@layout/item_timeline_recommendations" />

    </androidx.swiperefreshlayout.widget.SwipeRefreshLayout>

    <!-- Map Container with MapView - Full screen, behind header/dayFilter -->
    <FrameLayout
        android:id="@+id/mapContainer"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:visibility="gone"
        tools:visibility="visible"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintTop_toTopOf="parent">

        <com.tripian.trpcore.util.widget.MapView
            android:id="@+id/mapView"
            android:layout_width="match_parent"
            android:layout_height="match_parent" />

    </FrameLayout>

    <!-- Near Me Button (for location-based suggestions) -->
    <com.google.android.material.button.MaterialButton
        android:id="@+id/btnNearMe"
        style="@style/Widget.MaterialComponents.Button.OutlinedButton"
        android:layout_width="wrap_content"
        android:layout_height="40dp"
        android:layout_margin="16dp"
        android:textColor="@color/trp_timeline_primary"
        android:textSize="12sp"
        android:visibility="gone"
        app:cornerRadius="20dp"
        app:icon="@drawable/ic_near_me"
        app:iconTint="@color/trp_timeline_primary"
        app:layout_constraintBottom_toTopOf="@id/fabAddPlan"
        app:layout_constraintStart_toStartOf="parent"
        app:strokeColor="@color/trp_timeline_primary"
        tools:text="Near Me" />

    <!-- Empty State -->
    <LinearLayout
        android:id="@+id/emptyStateView"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:orientation="vertical"
        android:padding="32dp"
        android:visibility="gone"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintTop_toBottomOf="@id/btnSavedPlans">

        <ImageView
            android:layout_width="120dp"
            android:layout_height="120dp"
            android:contentDescription="Empty"
            android:src="@drawable/ic_empty_timeline" />

        <TextView
            android:id="@+id/tvEmptyTitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="16dp"
            android:fontFamily="@font/medium"
            android:textColor="@color/trp_timeline_title_color"
            android:textSize="18sp"
            tools:text="No plans yet" />

        <TextView
            android:id="@+id/tvEmptySubtitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:gravity="center"
            android:textColor="@color/trp_timeline_subtitle_color"
            android:textSize="14sp"
            tools:text="Tap the button below to add your first plan" />

    </LinearLayout>

    <!-- Map Toggle FAB - Liste gorunumunde gorunur -->
    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/fabMap"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginEnd="16dp"
        android:layout_marginBottom="16dp"
        android:contentDescription="Map"
        android:src="@drawable/ic_map"
        app:backgroundTint="@color/trp_text_primary"
        app:elevation="4dp"
        app:fabCustomSize="52dp"
        app:layout_constraintBottom_toTopOf="@id/fabAddPlan"
        app:layout_constraintEnd_toEndOf="parent"
        app:shapeAppearanceOverlay="@style/ShapeAppearanceOverlay.MaterialComponents.FloatingActionButton"
        app:tint="@color/trp_white" />

    <!-- Add Plan FAB - Liste gorunumunde gorunur -->
    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/fabAddPlan"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginEnd="16dp"
        android:layout_marginBottom="32dp"
        android:contentDescription="Add Plan"
        android:src="@drawable/ic_plus_bold"
        app:backgroundTint="@color/trp_pink700"
        app:elevation="4dp"
        app:fabCustomSize="52dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:shapeAppearanceOverlay="@style/ShapeAppearanceOverlay.MaterialComponents.FloatingActionButton"
        app:tint="@color/trp_white" />

    <!-- List Toggle FAB - Map gorunumunde gorunur (Add Plan yerine) -->
    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/fabList"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginEnd="16dp"
        android:layout_marginBottom="32dp"
        android:contentDescription="List"
        android:src="@drawable/ic_list"
        android:visibility="gone"
        app:backgroundTint="@color/trp_text_primary"
        app:elevation="4dp"
        app:fabCustomSize="52dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:shapeAppearanceOverlay="@style/ShapeAppearanceOverlay.MaterialComponents.FloatingActionButton"
        app:tint="@color/trp_white" />

    <!-- Map Bottom Item List - Horizontal list at bottom of map -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rvMapBottomList"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="32dp"
        android:clipToPadding="false"
        android:orientation="horizontal"
        android:paddingHorizontal="16dp"
        android:translationY="200dp"
        android:visibility="gone"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        tools:visibility="visible" />

</androidx.constraintlayout.widget.ConstraintLayout>
