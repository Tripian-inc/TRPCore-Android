org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
android.useAndroidX=true
android.enableJetifier=true
kotlin_version=2.0.21

# Mapbox Downloads Token - Required for building
# Set via environment variable or local.properties (not committed)
# Get from https://account.mapbox.com/access-tokens/ with DOWNLOADS:READ scope
# MAPBOX_DOWNLOADS_TOKEN=your_token_here
